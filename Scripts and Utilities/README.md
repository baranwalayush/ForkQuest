# Install packages
To install the required packages for the scripts and utilities in this repository, follow the instructions below:
**Using pip**:
   You have a `requirements.txt` file, you can install all the necessary packages by running the following command in your terminal or command prompt:
   ```
   pip install -r requirements.txt
   ```